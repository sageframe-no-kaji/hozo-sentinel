# Session cookie signing secret. 64 hex characters.
# Generate with:  python -c "import secrets; print(secrets.token_hex(32))"
# When set, hozo uses this and never writes a secret into config.yaml.
# When unset, hozo generates one on first boot and stores it in config.yaml.
HOZO_SESSION_SECRET=
